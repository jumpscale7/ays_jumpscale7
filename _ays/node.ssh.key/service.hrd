description='is a node running linux os'

#optional category of service, default = app
category=node

#optional parent on wich this service is deployed e.g. app on top of node, portforward rule on top of firewall
parent=

producer.1=
    name:'agent',
    action:'manage',
    descr:'manage 1 linux os',
    nr.min:0,
    nr.max:1,

